# OpenCode Slack bot configuration
# Get Slack values from https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# Signing Secret (from Basic Information page)
SLACK_SIGNING_SECRET=your-signing-secret

# Base port for local OpenCode SDK server instances (default 60000)
OPENCODE_PORT_BASE=60000
